const About = () => {

    return (
        <>
            <h2>About Mero Task Tracker</h2>
            <p>Version 1.0.0</p>
            <br />
            <p>
                Mero Task Tracker is a simple SPA for tracking tasks inspired from react crash course.
                It is built using React & Json-Server which mocks like a REST API server and uses
                json file persisting data just like SQLite but using JSON file.
            </p>
            <br />
            <p><a href="/">Go Back</a></p>
        </>
    )
}

export default About